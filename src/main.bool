main()
vars numUm, numDois, numTres, p, n, m
begin
	numUm = 40
	numDois = 20
	
	if numUm lt numDois then
		numTres = numUm + numDois
	else
		numTres = numUm - numDois
	end-if

	 p = new Pessoa
	 a = p

	 n = new Num
     b = n.retorna()
end